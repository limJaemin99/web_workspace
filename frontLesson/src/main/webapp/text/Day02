● Day02 _ 07.25

1. HtmlLesson2.html
	- CSS 연습
2. StyleSample.html
	- Style CSS 연습
3. HtmlLesson3.html
	- table 태그로 표를 만들고 style 태그로 스타일 지정하기
4. example2.html
	- 개인 연습
5. GallerySample.html
		+ gallery.css
	- CSS 와 div 태그 활용 연습


[오늘 사용한 CSS]
● 색상
	color
	background-color
	
● 테두리
	border-XXX(-XXX)
	border-style
	border-color
	
● 테두리 안쪽(내부) 여백
	padding-XXX
	
● 테두리 바깥쪽(외부) 여백
	margin-XXX
	
	
# 테두리 속성은 다음과 같이 한번에 작성할 수 있다.
	border-width: 1px; border-style: solid; border-color: black;
		▶ border: 1px solid black

# 테두리는 상,하,좌,우 방향에 대해 width, style, color 각각 설정할 수 있다.
	ㄴ 방향을 정하지 않고 border 로 지정할 경우 4방향 전부 설정
	border-top-width
	border-bottom-width
	border-left-width
	border-right-width



# 테두리 안쪽(내부)여백은 padding
   ㄴ 4개의 방향을 직접 지정할 수도 있고, 모두 동일하다면 방향을 생략하고 쓴다.
	padding-top
	padding-bottom
	padding-left
	padding-right

	padding: 40px 30px 20px 10px;	▶ 시계방향으로 위,오른쪽,아래,왼쪽
	padding: 40px 20px;				▶ 첫번째 값은 위,아래/두번째 값은 좌,우
	padding: 20px					▶ 모든 방향을 동일하게 지정

# 테두리 바깥쪽(외부)여백은 margin
   ㄴ ★margin은 특별히 auto를 설정해서 좌/우 여백을 중앙으로 배치되게 한다.
	margin-top
	margin-bottom
	margin-left
	margin-right


# width 는 너비, height 는 높이 ▶ 절대값으로 px 등을 줄 수 있고, 부모의 너비 기준으로 상대값으로 %(백분율)을 줄 수 있다.

# 일반적인 블록 요소 div, 인라인 요소 span
# width, height 는 블록 요소에만 설정 가능하다.
# 인라인 요소에 너비와 높이를 주고 싶으면 display 속성을 inline-block; 으로 값을 준다.
	# 인라인 요소 ⊃ 인라인 요소	▶ O
	# 인라인 요소 ⊃ 블록 요소		▶ X
	# 블록 요소 ⊃ 인라인 요소		▶ O
	# 블록 요소 ⊃ 블록 요소		▶ O
	
	
# style 태그 안에서 값을 줄 때는 ★선택자★ 를 사용한다.
# td:nth-child(3) 처럼 n번째 자식을 선택할 수 있다.


[오늘 해야 할 내용]
● practice - test2.html 완성하기